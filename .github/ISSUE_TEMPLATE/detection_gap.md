---
name: Detection gap
about: RepoScope missed or misclassified a framework, route, storage layer, entry point or import
labels: analyzer
---

**Repository** (public URL)

**What was missed or misclassified?**
e.g. "`src/handlers` was typed as Module but it's the API layer", "Django routes in `urls.py` are not detected", "`from app.core import x` did not resolve".

**Where is the evidence in the repo?** (file paths, manifest entries)
